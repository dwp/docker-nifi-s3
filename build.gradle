plugins {
    id 'java'
    id 'application'
}

mainClassName = "uk.gov.dwp.dataworks.GetTemplateFromS3"

jar {
    zip64=true
    manifest {
        attributes(
                'Main-Class': mainClassName

        )
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

group = 'uk.gov.dwp.dataworks'

repositories {
    mavenCentral()
    mavenLocal()
    maven {
        url = "https://jitpack.io"
    }
}

tasks.withType(JavaExec) {
    systemProperties System.properties
}

dependencies {
    implementation platform('com.amazonaws:aws-java-sdk-bom:1.11.1016')
    implementation 'com.amazonaws:aws-java-sdk-core'
    implementation 'com.amazonaws:aws-java-sdk-s3'
    implementation 'com.amazonaws:aws-java-sdk-SdkClientException'
    implementation group: 'commons-io', name: 'commons-io', version: '2.6'
}
